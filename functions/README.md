# Optional Cloud Functions (Admin SDK)

Client-side Firebase cannot delete other users' Authentication accounts.
If you need supervisors to delete Auth users, implement a callable function with Admin SDK.
